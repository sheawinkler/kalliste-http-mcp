services:
  mindsdb-http-proxy:
    command: >
      sh -lc '
        pip install fastmcp -U pip &&
        pip install -U fastmcp fastapi uvicorn httpx anyio &&
        python -m uvicorn scripts.mindsdb_http_proxy:app --host 0.0.0.0 --port 8004
      '
